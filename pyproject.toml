[project]
name = "tomakuro"
version = "0.1.0"
description = "A minimalist Telegram pomodoro timer bot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot[job-queue]>=22.6",
]
